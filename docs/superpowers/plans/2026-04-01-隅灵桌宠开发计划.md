# 隅灵 (Yú Líng) 桌宠分阶段开发计划

- **版本**: 1.0.0
- **日期**: 2026-04-01
- **作者**: Codex Agent
- **关联设计**: `docs/superpowers/specs/2026-04-01-隅灵桌宠设计.md`

## 阶段概览
| 阶段 | 目标 | 预计耗时 |
| --- | --- | --- |
| 阶段 0 · 环境基线 | 搭好跨平台开发/构建环境，建立仓库与基本 CI | 1-2 天 |
| 阶段 1 · 幻影显形 | 完成透明窗口、托盘、PlatformBridge 基础 | 3-4 天 |
| 阶段 2 · 灵动交互 | 搭建 Motion 系统、跨屏拖拽、交互菜单 | 4-5 天 |
| 阶段 3 · 神兽之语 | Rust 提醒内核 + 提醒列表提醒 UI | 4-5 天 |
| 阶段 4 · 发布验收 | 跨平台打包、性能/稳定性验证、签名 | 3 天 |

每个阶段内的任务清单均采用 Todo 形式，执行时由开发者勾选。建议按阶段顺序推进，不跨阶段并行，除非另有资源。

---

## 当前进度快照（2026-04-03）
- **当前阶段**：阶段 3 · 神兽之语（进行中）
- **已完成阶段**：阶段 0、阶段 1、阶段 2 的主线目标已完成；阶段 2 尚有 1 个低优先级遗留项未补。
- **当前主线任务**：补齐提醒内核的锁屏补偿与系统通知能力。
- **当前代码状态**：
  - 托盘“提醒列表”已切换为独立 `reminder` 窗口，主窗口仅保留宠物本体与提醒气泡，常态窗口为 `168×168`，提醒触发时临时扩展。
  - 提醒表单/列表已从主窗口迁出，当前采用固定尺寸、不可缩放的独立提醒窗口。
  - Rust 提醒内核已具备 SQLite + CRUD + fired/updated 事件广播，并改为 `sleep_until` + 动态唤醒调度。
  - `src/assets/motion/frames` 的 4 张 idle PNG 已按统一外接框裁掉透明边，当前素材尺寸统一为 `825×1040`，用于减少主窗口视觉空白。
  - 主窗口对 PNG 帧图已改为等比完整展示并贴底对齐，避免高挑帧在 `168×168` 常态窗口内被 `cover` 裁切。
- **完成当前阶段前还缺什么**：
  - 锁屏补偿 / 系统通知两项内核能力补齐。
  - Rust / 前端单元测试补齐，API 注释与 README 更新。
- **下一步建议顺序**：
  - 先处理锁屏补偿、系统通知。
  - 最后补测试与文档，收口阶段 3。

## 进度判断规则
- 看“阶段概览”只判断大阶段。
- 看“当前进度快照”判断今天应该做什么。
- 看各阶段 Todo 判断具体实现项是否完成。
- 若代码状态与 Todo 勾选不一致，以代码实际行为为准，并及时回写文档。

---

## 阶段 0 · 环境基线
- [x] 初始化 Git 仓库，设定 `.gitignore`（2026-04-01）。
- [x] 在 macOS 开发机安装 Node.js 20.19.0、pnpm 10.11.1、Rust 1.94.1、Tauri CLI 2.10.1，并记录安装命令（Windows 设备待配置）。
- [x] macOS：确认 Xcode Command Line Tools 存在；Apple ID 证书待正式签名阶段补充。Windows：VS Build Tools + WebView2 待后续准备。
- [x] 创建 `docs/`、`src/`、`src-tauri/` 目录结构，放入设计与计划文档（2026-04-01）。
- [x] 规划分支策略并编写 `CONTRIBUTING.md`（包含分支/提交/测试规范）。
- [ ] （可选）配置基础 CI（GitHub Actions）执行 `pnpm lint`、`cargo fmt --check`、`cargo clippy`。

**完成判定**：两端环境均可运行 `cargo tauri dev`（即使界面暂为空白），CI 通过。

---

## 阶段 1 · 幻影显形（壳体 + PlatformBridge 基础）
- [x] `pnpm create tauri-app` 初始化项目，改为 Vue 3 + JavaScript + Vite（2026-04-01）。
- [x] 在 `tauri.conf.json` 启用透明 / 置顶 / 无边框、托盘图标等桌宠特性（2026-04-01）。
- [x] Rust `platform_bridge` 模块：
  - [x] 定义 `MonitorDescriptor` 结构并通过命令返回 `available_monitors()`。
  - [x] 采用后台轮询 + 事件总线实现 `platform:monitors-updated` 广播。
  - [x] 实现 `logical_to_physical` / `physical_to_logical` 命令。
- [x] 托盘与开机自启：
  - [x] 添加系统托盘菜单（显示窗口 / 退出 / 切换自启）。
  - [x] 暴露 `get_autostart` / `set_autostart` 命令并与托盘开关同步。
- [x] 创建最小前端容器：透明背景 + 玻璃态卡片，展示显示器列表与自启开关。
- [x] 写初步手动测试表：在 macOS 上运行 `pnpm tauri dev` 验证透明窗口、托盘交互、自启事件；`pnpm build` 通过。

**完成判定**：透明置顶窗口可在双平台启动，托盘可显示/退出，监视器列表在热插拔时自动更新。

---

## 阶段 2 · 灵动交互（动画 + 拖拽 + 菜单）
- **阶段状态**：主线已完成，保留 1 个后续补齐项。
- [x] 定义 `assets/motion/manifest.json` 模板与 `MotionManifest` JS 描述对象（2026-04-01，使用 CSS 占位帧）。
- [x] 自研 MotionController（基于 `requestAnimationFrame`）：
  - [x] 解析 Manifest、驱动占位动画（CSS 几何）。
  - [x] 根据状态 / 低功耗模式调整 fps。
- [x] 导入首批 PNG 序列帧（idle 4 帧，占位）并接入渲染（2026-04-02）。
  - [x] 统一裁掉 4 张 idle 帧的透明边，保留主体并避免逐帧抖动（2026-04-03）。
- [x] 交互层：
  - [x] 实现拖拽监听（鼠标/触控），拖拽中切换 `drag` 状态。
  - [x] 与 PlatformBridge 提供的坐标转换协作（2026-04-02，使用 fallbackManualDrag 实现手动拖拽，后续补充 spring easing + 吸附）。
  - [x] 单击反应动画、双击打开"提醒列表"占位菜单。
- [x] Pinia 状态：集中管理 `monitors`、`powerMode`、拖拽状态等。
- [x] 低功耗模式：
  - [x] 提供手动切换，MotionController 自动降 fps。
  - [ ] 监听 `platform:session` 事件，锁屏 5 分钟后切 `sleep` 状态（待后续实现）。
- [x] 提醒列表 UI（占位版）：显示状态信息、手动切换低功耗模式、触发提醒占位动画。
- [x] 更新测试：手动验收拖拽、模式切换、提醒脉冲效果；记录备注（2026-04-02，验证 cargo check 和 pnpm build 通过）。

**完成判定**：角色可丝滑显示 idle/react/drag，跨屏拖拽无跳跃，提醒列表可打开并切换模式。

---

## 阶段 3 · 神兽之语（提醒系统 + 提醒列表功能）
- **阶段状态**：进行中，独立提醒窗口与精准调度已完成，当前转向锁屏补偿与系统通知。
- [ ] Rust `reminder_engine` 模块：
  - [x] 建立 SQLite `reminders.db`，定义表结构（含重复/延迟字段）（2026-04-02）。
  - [x] 编写 CRUD 命令：`reminder_create/update/delete/snooze/complete`（2026-04-02）。
  - [x] 使用 `tokio::time::sleep_until` 构建精准调度，并在 CRUD 后动态唤醒重排（2026-04-03）。
  - [ ] 处理 `platform:session locked/unlocked` 事件，补偿漏掉的提醒。
  - [ ] 触发时走系统通知（Toast/UserNotifications），失败回退（目前改为事件 + UI 气泡提醒，待系统通知插件评估）。
- [ ] 前端提醒体验：
  - [x] 提醒列表菜单升级为提醒列表 + 新建表单（含验证；初版以悬浮面板呈现，2026-04-03 已迁移到独立窗口）（2026-04-02）。
  - [x] `reminder_pulse` 动画层、毛玻璃气泡 + 文字提示（与 fired 事件联动，自动高亮宠物）（2026-04-02）。
  - [x] 气泡按钮（完成/延后/删除）对接命令（2026-04-02）。
  - [x] 托盘“提醒列表”菜单可唤起提醒管理界面；当前已切换为独立提醒窗口（2026-04-03）。
  - [x] 精简主舞台，仅保留宠物主体与提醒气泡，移除额外背景以符合最小驻留诉求（2026-04-02）。
  - [x] 提醒面板默认居中并与宠物解耦，支持 ESC/遮罩关闭及对焦（2026-04-02，过渡方案，现已由独立窗口替代）。
  - [x] 提醒面板可拖拽与 8 方向缩放，带玻璃质感和进场动画（2026-04-02，过渡方案，现已由独立窗口替代）。
  - [x] 新增提醒独立窗口（reminder WebView），与宠物主窗口完全解耦（2026-04-03）。
  - [x] 重构提醒 UI，使宠物窗口仅承担宠物展示，提醒表单/列表迁移到新窗口（2026-04-03）。
- [x] 状态同步：Pinia 订阅 `reminder:*` 事件，更新 UI 与托盘提示（2026-04-02）。
- [ ] 编写单位测试：
  - [ ] Rust：调度补偿、重复规则、数据库读写。
  - [ ] 前端：MotionController 状态切换守卫、提醒表单验证。
- [ ] 更新文档：补充 API 注释、README 运行方式。

**完成判定**：可在前端创建提醒、按时触发系统通知和气泡，并能完成/延后；测试全部通过。

---

## 阶段 4 · 发布与验收
- [ ] 性能验证：
  - [ ] 使用 Chrome DevTools/Activity Monitor 记录正常 vs 低功耗 CPU/GPU 占用，目标降低 ≥40%。
  - [ ] 长时间运行（≥2 小时）监测内存泄漏。
- [ ] 稳定性测试：
  - [ ] 多屏热插拔、DPI 更改、锁屏/解锁、系统睡眠唤醒。
  - [ ] Reminders 在跨日/跨周场景下的准确度。
- [ ] 打包：
  - [ ] macOS：`cargo tauri build` + 签名 + Notarize，验证首次安装、托盘功能。
  - [ ] Windows：生成安装包（MSIX/NSIS），验证 WebView2 依赖、开机自启注册。
- [ ] 发布资产：整理 CHANGELOG、安装步骤、系统要求截图，上传到仓库 Release。
- [ ] 编写用户快速上手指南（含提醒示例、快捷操作）。
- [ ] 回顾会议：总结风险、后续功能（AI 对话、更多动画）排期。

**完成判定**：双平台安装包通过签名/Notarization，性能和稳定性测试记录齐备，并完成版本发布说明。

---

## 使用说明
- 执行时建议每完成一条 Todo 即在文件中将 `- [ ]` 改为 `- [x]` 并附加日期。
- 如需增减阶段，可在该文件追加章节，保持“阶段名称 + Todo 列表 + 完成判定”结构。
- 每个阶段结束建议提交一次文档更新，以保留里程碑快照。
